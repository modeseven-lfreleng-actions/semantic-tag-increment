---
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2025 The Linux Foundation

name: 'CodeQL'
# Named codeql.yml NOT codeql.yaml (GitHub security UI will ignore it)

on:
  workflow_dispatch:
  push:
    branches: ['main', 'master']
    paths:
      - '!.github/**'
      - '!.docs/**'
  schedule:
    - cron: '40 4 * * 0'

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions: {}

jobs:
  scan:
    name: 'Audit Repository'
    # yamllint disable-line rule:line-length
    uses: lfit/releng-reusable-workflows/.github/workflows/reuse-python-codeql.yaml@f7aae218f1fa0257e3dcd19f5c7d3f88f482f266 # v0.10.1
    permissions:
      security-events: write  # Upload CodeQL scan results
      packages: read  # Fetch internal or private CodeQL packs
      actions: read  # Only required for workflows in private repositories
      contents: read
